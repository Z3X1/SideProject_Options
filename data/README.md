# Data Directory

This directory stores Deribit CSV snapshot exports.

CSV files are excluded by `.gitignore` by default.
To include raw data in version control, remove the corresponding rule from `.gitignore`.
